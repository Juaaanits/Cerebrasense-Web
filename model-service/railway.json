{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "watchPatterns": ["/model-service/**"]
  },
  "deploy": {
    "startCommand": "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
  }
}
